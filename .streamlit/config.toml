[theme]
primaryColor = "#00d4ff"          # Cor principal (ciano)
backgroundColor = "#0a0e27"       # Fundo principal (quase preto)
secondaryBackgroundColor = "#1a1f3a"  # Fundo secundário
textColor = "#f0f2f6"            # Texto claro
font = "sans serif"
